/**
 * Generated by Apache Royale Compiler from mx/events/ErrorEvent.as
 * mx.events.ErrorEvent
 *
 * @fileoverview
 *
 * @suppress {missingRequire|checkTypes|accessControls}
 */

goog.provide('mx.events.ErrorEvent');
/* Royale Dependency List: XML*/

goog.require('org.apache.royale.events.Event');



/**
 *  Constructor.
 *
 *  @asparam type The event type; indicates the action that caused the event.
 *
 *  @asparam bubbles Specifies whether the event can bubble
 *  up the display list hierarchy.
 *
 *  @asparam cancelable Specifies whether the behavior
 *  associated with the event can be prevented.
 *  
 *  @langversion 3.0
 *  @playerversion Flash 9
 *  @playerversion AIR 1.1
 *  @productversion Royale 0.9.3
 * @constructor
 * @extends {org.apache.royale.events.Event}
 * @param {string} type
 * @param {boolean=} bubbles
 * @param {boolean=} cancelable
 * @param {string=} text
 * @param {number=} id
 */
mx.events.ErrorEvent = function(type, bubbles, cancelable, text, id) {
  bubbles = typeof bubbles !== 'undefined' ? bubbles : false;
  cancelable = typeof cancelable !== 'undefined' ? cancelable : false;
  text = typeof text !== 'undefined' ? text : "";
  id = typeof id !== 'undefined' ? id : 0;
  mx.events.ErrorEvent.base(this, 'constructor', type, bubbles, cancelable);
  this.text = text;
};
goog.inherits(mx.events.ErrorEvent, org.apache.royale.events.Event);


/**
 * Prevent renaming of class. Needed for reflection.
 */
goog.exportSymbol('mx.events.ErrorEvent', mx.events.ErrorEvent);


/**
 * @export
 * @const
 * @type {string}
 */
mx.events.ErrorEvent.ERROR = "error";


/**
 * @export
 * @type {string}
 */
mx.events.ErrorEvent.prototype.text;


/**
 * Metadata
 *
 * @type {Object.<string, Array.<Object>>}
 */
mx.events.ErrorEvent.prototype.ROYALE_CLASS_INFO = { names: [{ name: 'ErrorEvent', qName: 'mx.events.ErrorEvent', kind: 'class' }] };



/**
 * Reflection
 *
 * @return {Object.<string, Function>}
 */
mx.events.ErrorEvent.prototype.ROYALE_REFLECTION_INFO = function () {
  return {
    variables: function () {
      return {
        'text': { type: 'String', get_set: function (/** mx.events.ErrorEvent */ inst, /** * */ v) {return v !== undefined ? inst.text = v : inst.text;}}
      };
    },
    methods: function () {
      return {
        'ErrorEvent': { type: '', declaredBy: 'mx.events.ErrorEvent', parameters: function () { return [ 'String', false ,'Boolean', true ,'Boolean', true ,'String', true ,'int', true ]; }}
      };
    }
  };
};
/**
 * @const
 * @type {number}
 */
mx.events.ErrorEvent.prototype.ROYALE_COMPILE_FLAGS = 26;
